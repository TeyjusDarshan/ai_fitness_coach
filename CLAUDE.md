# AI Fitness Coach

## Database Schema (Supabase)

Project: `khwrrvmejvrupokvfwvf` ("AI powered Fitness trainer", region `ap-northeast-1`, Postgres 17.6).

> ⚠️ **RLS is disabled on every table below.** All 14 tables are exposed to the `anon`/`authenticated` roles — anyone with the publishable key can read or write every row. This was originally tolerable while the data was public reference content (exercise library); that condition has now been **triggered**, not just theoretical — `user_profiles`, `sessions`, `session_exercises`, `session_day_logs`, `session_exercise_set_logs`, `session_exercise_rpe`, `user_joint_pain`, and `session_day_analysis` hold real per-user data (generated profiles, workout sessions, prescribed exercises, workout-day progress, per-set rep logs, RPE, reported joint pain, generated LLM workout summaries) and are still exposed with no RLS. This is a known, accepted gap pending an auth layer, not an oversight. Remediation SQL is available on request — don't apply it blind, since enabling RLS with no policies blocks all access.

> The exercise-library schema (`movement_types`, `equipment`, `joints`, `exercises`, `exercise_loaded_joints`, `exercise_relationships`) is sourced from `utilities/workout_builder/data/exercises.csv` and `relationships.csv`, migrated via `utilities/scripts/migrate_exercises_to_supabase.py` (idempotent — safe to re-run after editing the CSVs). Populated as of this writing: 66 exercises, 203 joint links, 110 relationships.

### Entity overview

`exercises` is the hub table. `movement_types`, `equipment`, and `joints` are lookup tables. `exercise_loaded_joints` is a many-to-many join between `exercises` and `joints`. `exercise_relationships` is a self-referential edge table (progression/regression links between exercises). `user_profiles` holds one generated profile per user; `user_joint_pain` holds one row per (user, joint) the user has reported pain for; `sessions` holds one row per generated workout plan for a user; `session_exercises` is a join table between `sessions` and `exercises` recording the prescribed sets/reps per exercise slot; `session_exercise_set_logs` and `session_exercise_rpe` track per-set rep completion and per-exercise RPE against a `session_exercises` row; `session_day_logs` tracks start/completion timestamps for each individual day within a session's plan; `session_day_analysis` holds one LLM-generated Tanglish summary per (session, day), written by the standalone `workout_analysis_consumer` service (see below).

```
movement_types ─┐
                ├─< exercises >─┬─< exercise_loaded_joints >─ joints
equipment ──────┘               └─< exercise_relationships >─ exercises (self-referential: from/to)
                                 └─< session_exercises >─ sessions >─ user_profiles ─< user_joint_pain >─ joints
                                       ├─< session_exercise_set_logs
                                       └─< session_exercise_rpe
                                                             sessions ─< session_day_logs
                                                             sessions ─< session_day_analysis
```

### Lookup tables

| Table | Columns | Notes |
|---|---|---|
| `movement_types` | `id` PK, `name` unique | squat, push, pull, hinge, lunge, carry, rotation |
| `equipment` | `id` PK, `name` unique | bodyweight, resistance bands |
| `joints` | `id` PK, `name` unique | ankle, knee, hip, spine, shoulder, elbow, wrist |

### `exercises` — hub table

Maps to `exercises.csv`. The CSV's `loaded_joints` (semicolon-delimited) is normalized into `exercise_loaded_joints`.

| Column | Type | Notes |
|---|---|---|
| `id` | int4 PK | `GENERATED BY DEFAULT AS IDENTITY` — CSV IDs can be inserted explicitly |
| `name` | varchar | |
| `movement_type_id` | int4, nullable, FK → `movement_types.id` | no cascade |
| `orientation` | varchar, nullable | check ∈ `horizontal, vertical` (NULL where CSV blank) |
| `dominant` | bool, nullable | |
| `equipment_id` | int4, nullable, FK → `equipment.id` | single-valued per exercise; no cascade |
| `min_reps` / `max_reps` | int2, nullable | rep range |
| `avoid_if` | text, nullable | contraindications |
| `on_board` | bool, default `true` | |
| `board_x` / `board_y` | float8, nullable | board coordinates |
| `created_at` | timestamptz, default `now()` | |

### `exercise_loaded_joints` — many-to-many join

Composite PK. Populated from the semicolon-delimited `loaded_joints` column in `exercises.csv`.

| Column | Type | Notes |
|---|---|---|
| `exercise_id` | int4, FK → `exercises.id` | `ON DELETE CASCADE` |
| `joint_id` | int4, FK → `joints.id` | no cascade |

PK: (`exercise_id`, `joint_id`).

### `exercise_relationships` — self-referential edges

Maps to `relationships.csv`. Each row links a `from` exercise to a `to` exercise with a direction.

| Column | Type | Notes |
|---|---|---|
| `id` | int4 PK | `GENERATED BY DEFAULT AS IDENTITY` |
| `from_exercise_id` | int4, FK → `exercises.id` | `ON DELETE CASCADE` |
| `to_exercise_id` | int4, FK → `exercises.id` | `ON DELETE CASCADE` |
| `type` | varchar | check ∈ `progression, regression` |
| `reason` | text, nullable | e.g. "Lack of strength", "RIR > 3 and hit rep ceiling" |

Unique: (`from_exercise_id`, `to_exercise_id`, `type`).

### `user_profiles` — generated client profile per user

Populated by the Flask `/workout-plan` endpoint (`backend/app.py`) via `WorkoutPlanRepository.upsert_user_profile`. **Upsert-by-`user_id`, not versioned** — each new generation for a user overwrites the previous profile; there is no history table.

| Column | Type | Notes |
|---|---|---|
| `user_id` | text PK | caller-supplied id (e.g. `"usr_10235"`), not a Supabase-auth UUID |
| `profile` | jsonb | full generated profile (`demographics`, `goals`, `experience`, `health`, `availability`, etc. — see `agents/mock_data_v1.py` for the shape) |
| `created_at` | timestamptz, default `now()` | |
| `updated_at` | timestamptz, default `now()` | bumped on every upsert |

### `user_joint_pain` — reported pain per (user, joint)

Populated by `WorkoutPlanRepository.upsert_joint_pain` / `delete_joint_pain` via `POST /api/users/<user_id>/joint-pain` and `DELETE /api/users/<user_id>/joint-pain/<joint_id>` (`backend/app.py`). Read by `_fetch_user_joint_pain` and fed into `get_injury_multiplier` to reduce load on exercises hitting a painful joint during progression.

| Column | Type | Notes |
|---|---|---|
| `id` | int4 PK | `GENERATED BY DEFAULT AS IDENTITY` |
| `user_id` | text, FK → `user_profiles.user_id` | `ON DELETE CASCADE` |
| `joint_id` | int4, FK → `joints.id` | no cascade |
| `pain_level` | varchar | check ∈ `mild, moderate, severe` |
| `created_at` | timestamptz, default `now()` | |
| `updated_at` | timestamptz, default `now()` | bumped on every upsert |

Unique: (`user_id`, `joint_id`) — makes `upsert_joint_pain` an idempotent upsert; re-reporting pain for a joint already on file overwrites it rather than duplicating.

### `sessions` — one row per generated workout plan

Populated by `WorkoutPlanRepository.create_session` (brand-new plan) or `WorkoutPlanRepository.create_progressed_session` (progress-plan flow — carries forward a completed session's exercises) after a successful generation. Plan-level metadata only — the per-exercise prescriptions live in `session_exercises`.

| Column | Type | Notes |
|---|---|---|
| `id` | int4 PK | `GENERATED BY DEFAULT AS IDENTITY` |
| `user_id` | text, FK → `user_profiles.user_id` | no cascade |
| `parent_session_id` | int4, nullable, FK → `sessions.id` | self-referential; `ON DELETE SET NULL`. `NULL` for a brand-new session (`create_session`); set to the source session's `id` when created via `create_progressed_session` (auto-progress on completion, or `POST /api/users/<user_id>/progress-plan`) |
| `plan_type` | varchar | `3_day` \| `4_day` |
| `plan_selection_reason` | text, nullable | |
| `medical_clearance_warning` | text, nullable | |
| `coach_notes` | text, nullable | |
| `safety_summary` | jsonb, nullable | `{excluded_movements, excluded_joints, excluded_equipment}` |
| `completed` | bool, default `false` | |
| `created_at` | timestamptz, default `now()` | |
| `completed_at` | timestamptz, nullable | set when marked completed |

Partial unique index `sessions_one_unfinished_per_user` on `(user_id)` where `completed = false` — enforces at most one unfinished session per user, matching the endpoint's short-circuit logic (`POST /workout-plan` returns the existing unfinished session instead of generating a new one) and guarding against duplicate inserts from a racing/retried request.

### `session_exercises` — join table between `sessions` and `exercises`

One row per prescribed exercise slot within a non-rest day of a session. Deliberately does **not** duplicate `name`/`movement_type`/`equipment`/`orientation`/`dominant` (joined in from `exercises` at read time) or `day_label`/`split_name`/`is_rest_day` (reconstructed from the fixed `DAY_TEMPLATES[plan_type]` skeleton in `agents/prompts_v1.py` using `sessions.plan_type` + `day_number`).

| Column | Type | Notes |
|---|---|---|
| `id` | int4 PK | `GENERATED BY DEFAULT AS IDENTITY` |
| `session_id` | int4, FK → `sessions.id` | `ON DELETE CASCADE` |
| `exercise_id` | int4, FK → `exercises.id` | no cascade |
| `day_number` | int2 | 1–7, matches `DAY_TEMPLATES[plan_type].schedule[].day_number` |
| `category` | varchar | check ∈ `primary, secondary` — which slot list this exercise fills |
| `slot_label` | varchar, nullable | e.g. `"Hinge"`, `"Push (Horizontal)"` — echoes the slot label from `DAY_TEMPLATES` |
| `sets` | int2, nullable | |
| `reps` | varchar, nullable | |
| `rep_range` | varchar, nullable | |
| `note` | text, nullable | |
| `created_at` | timestamptz, default `now()` | |

Per-set completion and RPE are tracked in the child tables below rather than on this row.

### `session_exercise_set_logs` — per-set completion within a `session_exercises` row

One row per set actually logged, written via `WorkoutPlanRepository.log_set`. Each `set_number` is its own row (keyed by the unique constraint below), so logging two different sets on the same exercise back to back can't race the way a shared jsonb array could — this replaced an earlier `session_exercises.completed_sets` jsonb boolean array design for that reason.

| Column | Type | Notes |
|---|---|---|
| `id` | int4 PK | `GENERATED BY DEFAULT AS IDENTITY` |
| `session_exercise_id` | int4, FK → `session_exercises.id` | `ON DELETE CASCADE` |
| `set_number` | int2 | 1-indexed, the kth set within that `session_exercises` row (≤ its `sets`) |
| `completed_reps` | int2 | reps actually performed on that set |
| `created_at` | timestamptz, default `now()` | |

Unique: (`session_exercise_id`, `set_number`) — one log row per set, and makes `log_set` an idempotent upsert. `complete_day` treats a `session_exercises` row as fully done once it has as many logged sets as its prescribed `sets`.

### `session_exercise_rpe` — perceived exertion per prescribed exercise

One row per `session_exercises` row, written via `WorkoutPlanRepository.log_rpe` — RPE is logged once per exercise (not per set).

| Column | Type | Notes |
|---|---|---|
| `id` | int4 PK | `GENERATED BY DEFAULT AS IDENTITY` |
| `session_exercise_id` | int4, FK → `session_exercises.id` | `ON DELETE CASCADE` |
| `rpe` | int2 | check 1–10 |
| `created_at` | timestamptz, default `now()` | |

Unique: (`session_exercise_id`) — makes `log_rpe` an idempotent upsert.

### `session_day_logs` — per-day progress within a session

Tracks start/completion timestamps for each individual day (`day_number`) of a session's plan, separately from `sessions.completed`/`completed_at` which track the whole multi-day plan. Populated by `WorkoutPlanRepository.start_day` / `complete_day` (`backend/repository/workout_plan_repository.py`), driving the PWA dashboard's per-day status (rest / completed / next / upcoming) and the end-of-day summary screen's Total Time stat.

| Column | Type | Notes |
|---|---|---|
| `id` | int4 PK | `GENERATED BY DEFAULT AS IDENTITY` |
| `session_id` | int4, FK → `sessions.id` | `ON DELETE CASCADE` |
| `day_number` | int2 | 1–7, matches `DAY_TEMPLATES[plan_type].schedule[].day_number` |
| `started_at` | timestamptz, nullable | set once, on first `start_day` call for this (session, day) — never overwritten by a later re-entry into the same day |
| `completed_at` | timestamptz, nullable | set when the day's summary screen is reached (`complete_day`) |
| `created_at` | timestamptz, default `now()` | |

Unique: (`session_id`, `day_number`) — enforces one log row per day per session, and makes `start_day`/`complete_day` idempotent upserts. When every non-rest `day_number` in a session's `DAY_TEMPLATES[plan_type]` has a `completed_at`, `complete_day` automatically calls `mark_session_completed` to close out the whole plan.

### `session_day_analysis` — LLM-generated Tanglish summary per (session, day)

One row per day of a session that's been marked complete. Written by the standalone `workout_analysis_consumer` package (repo root, alongside `backend`/`frontend`) — a Kafka consumer, independent of the Flask app, that listens on the `workout-complete-events` topic (produced by `backend/kafka_producer` when `POST /api/sessions/<session_id>/days/<day_number>/complete` succeeds). On each event it reads that day's `session_exercises` (with `session_exercise_set_logs` and `session_exercise_rpe` joined in), the user's `user_profiles.profile`, and their `user_joint_pain`, then prompts Gemini (`gemini-3.8-flash`) to write a short coach-style summary in **Tanglish** (Tamil-majority, code-switching into English for gym/fitness terms — see `workout_analysis_consumer/prompts.py`), and upserts the result here. If the session has a `parent_session_id`, the consumer also fetches that parent session's `session_exercises`/`session_exercise_set_logs`/`session_exercise_rpe`/`session_day_logs` for the **same `day_number`** and includes them as `previous_session` context, so the prompt can call out individual- and exercise-specific progress compared to the client's last time on that day of their split (see `AnalysisRepository._fetch_parent_session_context`).

| Column | Type | Notes |
|---|---|---|
| `id` | int4 PK | `GENERATED BY DEFAULT AS IDENTITY` |
| `session_id` | int4, FK → `sessions.id` | `ON DELETE CASCADE` |
| `day_number` | int2 | 1–7, matches `DAY_TEMPLATES[plan_type].schedule[].day_number` |
| `analysis` | text | the generated Tanglish summary |
| `audio_url` | text, nullable | URL of the generated voice analysis (TTS audio) for `analysis` |
| `created_at` | timestamptz, default `now()` | |

Unique: (`session_id`, `day_number`) — makes the consumer's write an idempotent upsert, so redelivering an already-processed Kafka message overwrites rather than duplicates.

### Notes on FK delete behavior

`movement_type_id` and `equipment_id` on `exercises`, `exercise_id` on `session_exercises`, and `joint_id` on `user_joint_pain`, have no cascade, so deleting a referenced row is blocked while referenced. `user_id` on `sessions` and on `user_joint_pain` is `ON DELETE CASCADE` — deleting a `user_profiles` row cascades to that user's `sessions` and `user_joint_pain` rows; `sessions` in turn cascades to `session_exercises`, `session_day_logs`, and `session_day_analysis` via `session_id`, and `session_exercises` cascades further to `session_exercise_set_logs` and `session_exercise_rpe` via `session_exercise_id`. So deleting a `user_profiles` row transitively deletes all of that user's data across every child table. The join/edge tables (`exercise_loaded_joints`, `exercise_relationships`) cascade on delete from `exercises`. `sessions.parent_session_id` (self-referential) is the one exception to the "no cascade blocks delete" rule: it's `ON DELETE SET NULL`, so deleting a parent session merely nulls out its children's `parent_session_id` rather than being blocked or cascading further. Indexes exist on all FK columns.
